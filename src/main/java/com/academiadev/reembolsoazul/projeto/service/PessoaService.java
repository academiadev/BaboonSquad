package com.academiadev.reembolsoazul.projeto.service;

import org.springframework.stereotype.Service;

import com.academiadev.reembolsoazul.projeto.dto.PessoaDTO;

@Service
public class PessoaService {

	public void cadastrar(PessoaDTO pessoaDto) {
		/*validaEmail(pessoaDto.getEmail());
		validaSenha(pessoaDto.getSenha());
		validaSenhaIgual(pessoaDto.getSenha(), pessoaDto.getSenhaRepetida());*/
		
	}

}
